# Veen Desk

Chief of Staff HQ for every product we build — live links, repos, and source chats in one place.

Live: deploy this repo to Vercel as `veen-desk`.

## Update inventory

Edit `products.js` and redeploy. Groups: `active`, `creator`, `practice`, `archive`.
